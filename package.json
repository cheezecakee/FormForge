{
    "name": "formforge",
    "private": true,
    "devDependencies": {
        "@types/bun": "latest",
        "@types/chrome": "^0.1.42"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "scripts": {
        "build": "bun build scripts/*.ts --outdir dist --root scripts"
    }
}
